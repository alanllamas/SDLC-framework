ontology_entry:
  id: "ONT_012"
  concept: "atomic-file-writes-posix-temp-rename-python"
  layer: "technical"
  captured_date: "2026-06-14"
  injected_by: "agent"
  confidence: "HIGH"
  stale_after_months: 36
  sources:
    - id: "posix-rename-atomic"
      type: "standard"
      title: "POSIX os.rename() atomicity guarantee on same filesystem"
    - id: "python-os-replace"
      type: "stdlib"
      title: "Python 3.3+ os.replace() — atomic on POSIX and Windows for overwrite"
      url: "https://docs.python.org/3/library/os.html#os.replace"
    - id: "sqlite-wal-pattern"
      type: "precedent"
      title: "SQLite WAL: write to temp log file, then checkpoint to main DB"
  informs_artifacts: ["TDR_007", "TREQ_007", "TREQ_028"]
  implementation_note: "Use os.replace() explicitly, not os.rename(). Create NamedTemporaryFile with dir=target.parent to guarantee same-filesystem atomicity. Windows-safe."
  finding: "CONFIRMS. Implementation refinement: os.replace() + dir=target.parent is the correct pattern."
